"""Run the eval set and record baseline numbers.

Measures extraction accuracy, explanation fidelity, correct refusal, and
needs-review correctness against the labelled synthetic samples in eval_set/.
Re-run on every change; regenerate the demo cache alongside.

See PROJECT_SPEC.md section 13.

TODO (Step 3 onward): build the eval harness once samples are labelled.
"""
