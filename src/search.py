"""Simple keyword and field search over stored records (v1).

Returns matching past records so the user can find and read back the right page,
presenting the original image, the structured record, and the stored
plain-language explanation. It never composes new medical content.
Natural-language chat retrieval is v2 and will sit behind the guard.

See PROJECT_SPEC.md sections 4 and 5.

TODO (Step 7): implement simple search.
"""
