# Core
streamlit>=1.40
anthropic>=0.40

# Schema and config
jsonschema>=4.23
python-dotenv>=1.0

# Image and document handling
pillow>=10.4
pymupdf>=1.24        # render PDF pages to images for vision extraction

# Export and backup
fpdf2>=2.7           # pure-Python PDF export of the doctor summary
cryptography>=42.0   # AES-GCM: backup passphrase encryption and per-user store encryption

# Cloud storage (hosted deploy only; local runs never import it)
boto3>=1.34          # Cloudflare R2 via its S3-compatible API

# Tests
pytest>=8.3
